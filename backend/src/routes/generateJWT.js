import express from 'express';
// import generateTokenController from '../controllers/generateTokenController.js';


let router = express.Router();

router.post("/v1/api/generate-token/",
  // generate
);

export default router;

